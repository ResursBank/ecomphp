# Guides: https://confluence.atlassian.com/x/e8YWN for more examples.

pipelines:
  default:
    - step:
        name: EComPHP 1.3 with PHP 5.4.45
        image: php:5.4.45
        caches:
          - composer
        script:
          - apt-get update && apt-get install -y unzip libxml2-dev git
          - docker-php-ext-install soap
          - docker-php-ext-enable soap
          - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
          - composer install
          - composer require phpunit/phpunit
          - vendor/bin/phpunit --configuration ./phpunit.xml
    - step:
        name: EComPHP 1.3 with PHP 7.2.3
        image: php:7.2.3
        caches:
          - composer
        script:
          - apt-get update && apt-get install -y unzip libxml2-dev git
          - docker-php-ext-install soap
          - docker-php-ext-enable soap
          - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
          - composer install
          - composer require phpunit/phpunit
          - vendor/bin/phpunit --configuration ./phpunit.xml
    - step:
        name: EComPHP 1.3 with PHP 7.1.11
        image: php:7.1.11
        caches:
          - composer
        script:
          - apt-get update && apt-get install -y unzip php-soap libxml2-dev git
          - docker-php-ext-install soap
          - docker-php-ext-enable soap
          - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
          - composer install
          - composer require phpunit/phpunit
          - vendor/bin/phpunit --configuration ./phpunit.xml
    - step:
        name: EComPHP 1.3 with PHP 5.5.38
        image: php:5.5.38
        caches:
          - composer
        script:
          - apt-get update && apt-get install -y unzip libxml2-dev git
          - docker-php-ext-install soap
          - docker-php-ext-enable soap
          - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
          - composer install
          - composer require phpunit/phpunit
          - vendor/bin/phpunit --configuration ./phpunit.xml
    - step:
        name: EComPHP 1.3 with PHP 5.6.23
        image: php:5.6.23
        caches:
          - composer
        script:
          - apt-get update && apt-get install -y unzip libxml2-dev git
          - docker-php-ext-install soap
          - docker-php-ext-enable soap
          - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
          - composer install
          - composer require phpunit/phpunit
          - vendor/bin/phpunit --configuration ./phpunit.xml
